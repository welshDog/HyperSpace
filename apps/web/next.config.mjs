/** @type {import('next').NextConfig} */
const nextConfig = {
  // We removed turbopack config since we're not using it anymore
  reactStrictMode: true,
  swcMinify: true
};

export default nextConfig;
